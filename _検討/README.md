# _検討｜作りかけを置く場所

本番（`prototype/`）に入れる前に、造形と寸法を確かめるための HTML を置く。
ここのファイルは**成果物ではなく、検討の記録**。

## 版番号は1本

不動産は以前、`間取り骨格` `間取り中身入り` `今のうち案` の3系列が
**別々に番号を走らせていた**。その結果、

- `間取り中身入りv23`（最新）
- `今のうち案v27`（こちらも最新）

が同時に成り立ち、どちらが最新か分からなくなった。番号の大きいほうが
古い、ということも起きる。

**領域ごとに通し番号を1本にする。** 系列を分けない。
不動産は `不動産v28.html` が現在地で、次は `不動産v29.html`。

v28 の番号は、旧2系列の大きいほう（今のうち案v27）の次を取った。
番号を戻すと、過去のコミットメッセージやメモの参照が全部ずれるため。

## いま置いてあるもの

| ファイル | 何か |
|---|---|
| `不動産v28.html` | **本番へ移した版。** 2026-09-20、`prototype/real-estate/render.js`・`area.css` へ移植済み。以後の不動産の造形検討は本番の上で行う（このファイルは移植元の記録として残す） |
| `今のうち案v27.html` | v28 の親。上段2部屋の中身（項目・文言・造形）はここから移植した |
| `間取り中身入りv22.html` | v28 のもう一つの親。間取りの枠（部屋割り・壁の生成）はここから |
| `間取り間仕切りv18.html` | **失敗として残してある。** 襖・腰壁の4段階を線幅の数値で作ろうとして、縮尺で見分けが付かなくなった |
| `間取り検討.html` | 部屋定義から壁を生成する仕組みの検討（骨格 v8） |
| `間取り割り当て.html` | 項目設計 1〜7 と部屋の対応 |
| `中身の実測.html` | 中身を幅を変えて流し込み、折り返しを測る |
| `care-board-*.html` | 介護のボード |

途中版は消した（git の履歴には残っている）。残したのは、
**現在地の直接の親**と、**意図して残された失敗**、**まだ参照される道具**。

## 消していい・消してはいけない

- 次の版を作ったら、**その親でなくなった版は消す**。並べて比べたいときは
  git で取り出す
- ただし**失敗の記録**（なぜその道を選ばなかったか）は残す。消すと同じ道を
  もう一度通る
- 外から参照されているものは、消す前に参照元を直す
  （`prototype/**/_次セッション指示*.md`、`prototype/assets/*.README.md`）
