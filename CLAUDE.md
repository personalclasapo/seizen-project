# SeiZen｜作業上の約束

素のHTML/CSS/JS。ビルドもフレームワークも入れていない。設計の正本は
`設計/SeiZen 全体設計.md`、プロトタイプの構成は `prototype/README.md`。

## 成果物UIのモチーフは SVG で描く

各領域の成果物UIは、現実の対象（通帳・証券・手帳・ベッド等）を
連想させる造形を持つ（正本 §4-2）。**この造形は SVG で描く。**

**`div` ＋ `border-radius` ＋ `linear-gradient` で代用しない。**
角丸の矩形に色を敷いただけのものは、その対象には見えない。ベッドは
茶色い長方形ではないし、手帳は緑の長方形ではない。

すでに出来ている基準は `contract-digital` の iPhone フレーム：

- 本体・面取り・画面・ダイナミックアイランドを層として重ねる
- ステータスバーの電波・Wi-Fi・電池は、専用の `viewBox` を持つ
  グリフとして1本ずつ描く（`render.js` の `.ph-wifi` `.ph-batt`）
- クレジットカードには EMV チップがある

同じ密度で、銀行口座には通帳、保険には証券フォルダの3面折り、
医療には手帳、介護にはベッドを描く。

### 自作するか、素材を持ってくるか ―― 着手前に判定する

「SVG で描く」は「パスを自分で書く」という意味ではない。医療の人体
シルエットは、自前のベジェで何度書き直しても腕が胴に飲まれる・脚が
棒になる、が直らず、最終的に Wikimedia の PD 素材を持ってきた
（`prototype/assets/body-front.README.md`）。**破綻した自作を置く
より、既製の PD/CC0 素材を使うほうが、この約束に適う。**

モチーフに手をつける前に、こう振り分ける：

1. **幾何プリミティブ数個（矩形・円・線）で、寸法を数個決めれば
   完成するか？**
   - Yes（iPhone のベゼル、通帳の背、封筒の三角折り）→ 自作可。
     ただし寸法は**実物の実寸比から引く**（クレジットカードなら
     85.6×54mm ≒ 1.585:1）。勘で置かない。
   - No（診察券、カードホルダー、お薬手帳、書類箱、人体、車、
     建物 ―― 「それらしさ」に説得力が要るもの）→ **PD/CC0 素材を
     探すのが先。** 無いときだけ自作を検討し、その判断を素材の
     README に残す。

2. **作り始めたあと、スクショを見て座標・px を1つずつ動かす作業を
   2回以上やったら、手を止めて素材探しに切り替える。** それは人体
   ループのサイン ―― 妥協の積み上げで質は出ない。

3. 素材を使ったら、`body-front.README.md` と同じ形で
   「出どころ・ライセンス・なぜこれを選んだか」を残す。

### CSS が受け持つもの

造形そのものではなく、**造形の置かれ方**：余白・段組み・色トークン・
状態による差分・レスポンシブ。SVG は「物の形」、CSS は「物の配置」。

質感の微調整（紙の地・影・木目の罫）を CSS で足すのは構わない。
ただし**物の輪郭を CSS の矩形で代用しない**のが線。

### 迷ったとき

「これは何の絵か」を一言で言えるか。言えなければ、まだ描けていない。

## 状態は正本 §11 に寄せる

「空欄」と「該当なし」を同じ扱いにしない。未確認／確認中／該当なし
などを別の状態として持つ。進捗は入力率ではなく、必要な状態がどこまで
成立しているかで数える（§12）。

## 領域をまたいで形を持ち出さない

一つの領域で作ったステップ・質問形式・造形を、他領域へ機械的に
横展開しない（正本 §13）。共有してよいのは外殻（`shared/`）と
状態の語彙まで。
